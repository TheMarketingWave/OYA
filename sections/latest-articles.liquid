{% style %}
  .latest-articles {
    width: 100%;
    padding: 60px 20px;
    padding-top: 140px;
    background: var(--color-brand-brown2);
    color: var(--color-brand-cream);
    overflow: hidden;
  }

  .latest-articles__container {
    max-width: 1200px;
    margin: 0 auto;
  }

  .latest-articles__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 30px;
    margin-bottom: 60px;
    position: relative;
  }

  .article-card {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .article-card__image {
    width: 100%;
    aspect-ratio: 1;
    overflow: hidden;
    border-radius: 4px;
  }

  .article-card__image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.3s ease;
  }

  .article-card__content {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .article-card__title {
    font-size: 24px;
    font-weight: bold;
  }

  .article-card__excerpt {
    font-size: 16px;
  }

  .latest-articles__cta {
    display: flex;
    justify-content: center;
  }

  .latest-articles__title {
    font-size: 48px;
    font-weight: bold;
    color: var(--color-brand-brown);
    text-transform: uppercase;
    position: absolute;
    top: -20px;
    left: 50%;
    transform: translate(-50%, -400%) scale(1);
  }

  @media screen and (max-width: 768px) {
    .latest-articles {
      padding-top: 120px;
    }

    .latest-articles__grid {
      grid-template-columns: 1fr;
    }
  }
{% endstyle %}

<section class="latest-articles">
  <div class="latest-articles__container">
    <div class="latest-articles__grid">
      <h2 class="latest-articles__title title-font">Journal</h2>
      {% assign selected_blog = blogs[section.settings.blog] %}
      {% if selected_blog %}
        {% for article in selected_blog.articles limit: 3 %}
          <article class="article-card">
            <div class="article-card__image">
              {% if article.image %}
                {{
                  article.image
                  | image_url: width: 400, height: 400
                  | image_tag: alt: article.image.alt, loading: 'lazy'
                }}
              {% else %}
                <div style="width: 100%; height: 100%; background: var(--color-brand-cream); opacity: 0.1;"></div>
              {% endif %}
            </div>

            <div class="article-card__content">
              <h3 class="article-card__title">
                <a href="{{ article.url }}" style="color: inherit; text-decoration: none;">
                  {{ article.title }}
                </a>
              </h3>

              {% if article.excerpt != blank %}
                <p class="article-card__excerpt">
                  {{ article.excerpt | strip_html | truncate: 120 }}
                </p>
              {% else %}
                <p class="article-card__excerpt">
                  {{ article.content | strip_html | truncate: 120 }}
                </p>
              {% endif %}
            </div>
          </article>
        {% endfor %}
      {% else %}
        <div style="grid-column: 1 / -1; text-align: center; color: var(--color-brand-cream); opacity: 0.6;">
          <p>Please select a blog in the section settings.</p>
        </div>
      {% endif %}
    </div>

    {% if selected_blog %}
      <div class="latest-articles__cta">
        {% render 'icon-button',
          icon: 'blog.svg',
          label: 'Discover Our Journal',
          url: selected_blog.url,
          variant: 'secondary'
        %}
      </div>
    {% endif %}
  </div>
</section>

{% schema %}
{
  "name": "Latest Articles",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "blog",
      "id": "blog",
      "label": "Blog"
    }
  ],
  "presets": [
    {
      "name": "Latest Articles"
    }
  ]
}
{% endschema %}
